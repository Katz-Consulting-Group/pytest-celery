version: '3.9'
services:
  worker:
    build:
      context: .
      args:
        - CELERY_VERSION=4.4.7
    volumes:
      - .:/app
    environment:
      - CELERY_BROKER_URL=memory://
      - CELERY_RESULT_BACKEND=cache+memory://
      # - LOG_LEVEL=DEBUG
